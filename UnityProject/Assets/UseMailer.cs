using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UseMailer : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Mailer.StartSelect("attachmentPath");
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
